<%= form_for @user do |f| %>
	<div>
		<%= f.text_field :first_name, placeholder: "first name" %>
	</div>
	<div>
		<%= f.text_field :last_name, placeholder: "last name" %>
	</div>
	<div>
		<%= f.text_field :email, placeholder: "email" %>
	</div>
	<div>
		<%= f.text_field :password, placeholder: "password" %>
	</div>
	<div>
		<%= f.text_field :password_confirmation, placeholder: "confirm password" %>
	</div>
	<div>
		<%= f.submit %>
	</div>

<% end %>

<!-- student, ga_staff, outsidertaco (rename to whatever), email, password_digest, about, linkedin_url, github_url, dribbble_url, behance_url, website_url, skills (tags), interest (tags), courses -->